<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Infos du site {{ site.name }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            font-size: 12px;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
    </style>
</head>
<body>
    <h1>Infos du site {{ site.name }}</h1>
    <table>
        <thead>
            <tr>
                <th>Nom</th>
                <th>Adresse</th>
                <th>Adresse (suite)</th>
                <th>Code Postal</th>
                <th>Ville</th>
                <th>Pays</th>
                <th>Téléphone</th>
                <th>Téléphone 2</th>
                <th>Fax</th>
                <th>Entreprise</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{ site.name }}</td>
                <td>{{ site.address }}</td>
                <td>{{ site.address2 }}</td>
                <td>{{ site.postalcode }}</td>
                <td>{{ site.city }}</td>
                <td>{{ site.country }}</td>
                <td>{{ site.tel }}</td>
                <td>{{ site.tel2 }}</td>
                <td>{{ site.fax }}</td>
                <td>{{ site.entreprise }}</td>
            </tr>
        </tbody>
    </table>
</body>
</html>
